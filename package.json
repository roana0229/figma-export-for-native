{
  "scripts": {
    "watch": "tsc-watch --onSuccess \"yarn build\"",
    "build": "figplug build -clean -O -o=plugin",
    "format": "prettier --write 'src/*.ts'"
  },
  "dependencies": {
    "@types/figma": "^1.0.2",
    "@types/jszip": "^3.1.7",
    "figplug": "^0.1.15",
    "jszip": "^3.2.2",
    "prettier": "^1.19.1",
    "tsc-watch": "^4.2.3",
    "typescript": "^3.8.3"
  }
}
